# Github Activity Summariser
CLI that takes a github username and returns a summary of their activity on github. It uses the github API to fetch user activity and then summarises the result.

## Usage

Run the ```main.py``` file. It will prompt you to provide a github username.